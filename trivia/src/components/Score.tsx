import React from 'react';
import { useSelector } from 'react-redux';

function Score({playerOneScore, playerTwoScore}: any) {
    // let currentValue: any = useSelector(state => state);
    // console.log(currentValue);
    return (
        <div className="App">
        <h1>Score</h1>
        </div>
    );
}
export default Score;
