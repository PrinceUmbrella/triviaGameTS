import React, {useState, useEffect} from 'react';

function InputHandler() {
	const [playerInfo, setPlayerInfo] = useState(5);
	return (
		<div className="App">
		    <h1>Input</h1>
            
		</div>
  );
}

export default InputHandler;
